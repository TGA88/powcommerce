import { distributorMcsProfileApi } from './distributor-mcs-profile-api';

describe('distributorMcsProfileApi', () => {
  it('should work', () => {
    expect(distributorMcsProfileApi()).toEqual('distributor-mcs-profile-api1');
  });
});
