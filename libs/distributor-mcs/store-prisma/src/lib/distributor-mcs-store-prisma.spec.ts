import { distributorMcsStorePrisma } from './distributor-mcs-store-prisma';

describe('distributorMcsStorePrisma', () => {
  it('should work', () => {
    expect(distributorMcsStorePrisma()).toEqual('distributor-mcs-store-prisma');
  });
});
