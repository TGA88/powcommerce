import { distributorMcsProfileCore } from './distributor-mcs-profile-core';

describe('distributorMcsProfileCore', () => {
  it('should work', () => {
    expect(distributorMcsProfileCore()).toEqual('distributor-mcs-profile-core');
  });
});
