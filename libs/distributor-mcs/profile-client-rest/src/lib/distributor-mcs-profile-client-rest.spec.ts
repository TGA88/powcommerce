import { distributorMcsProfileClientRest } from './distributor-mcs-profile-client-rest';

describe('distributorMcsProfileClientRest', () => {
  it('should work', () => {
    expect(distributorMcsProfileClientRest()).toEqual(
      'distributor-mcs-profile-client-rest'
    );
  });
});
