{
  "name": "pcm-distributor-web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pcm-distributor-web",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/pcm-distributor-web",
        "outputPath": "dist/apps/pcm-distributor-web"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/pcm-distributor-web"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "pcm-distributor-web:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "pcm-distributor-web:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "pcm-distributor-web:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "pcm-distributor-web:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/pcm-distributor-web/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/pcm-distributor-web/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
