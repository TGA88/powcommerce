{
  "name": "distributor-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/distributor-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/distributor-app",
        "outputPath": "dist/apps/distributor-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/distributor-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "distributor-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "distributor-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "distributor-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "distributor-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/distributor-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/distributor-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
